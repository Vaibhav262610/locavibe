/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i.pinimg.com", "mir-s3-cdn-cf.behance.net"], // Add more domains if needed
    },
};

export default nextConfig;
