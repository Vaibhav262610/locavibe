/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["i.pinimg.com", "mir-s3-cdn-cf.behance.net", "images.pexels.com"], // Add more domains if needed
    },
};

export default nextConfig;
